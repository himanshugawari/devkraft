import { checkEmail, getBase64, checkName, CheckPassword } from './validation';

export {
  checkEmail as emailVerification,
  getBase64 as convertImage,
  checkName as nameVerification,
  CheckPassword as passwordVerification,
};
